#include <iostream>
#include <time.h>
#include <stdlib.h>
#include <string>

using namespace std;

int main()
{
int number;
cout <<"enter the number of input";
cin >>number;
for(i=0;i<number;i++)
 {
  srand(time());
  rand();
 }
	
return 0;
}
